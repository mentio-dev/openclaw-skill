{
  "name": "@mentions/openclaw-skill",
  "version": "0.6.2",
  "private": true,
  "description": "The Mentio skill for OpenClaw, published to ClawHub as `mentio`: SKILL.md plus one rule file per API area, the reference parts generated from the OpenAPI document.",
  "type": "module",
  "scripts": {
    "generate": "tsx scripts/generate.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.23.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
